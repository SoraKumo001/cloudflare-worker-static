{
  "name": "cloudflare-worker-static",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "local:migration": "yarn wrangler d1 migrations apply db --local",
    "remote:migration": "yarn wrangler d1 migrations apply db --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "wrangler": "^3.78.10"
  }
}
